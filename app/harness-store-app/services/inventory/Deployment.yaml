apiVersion: apps/v1
kind: Deployment
metadata:
  name: store-inventory
  labels:
    app: store
    service: inventory
    release: "1.2.0"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: store
      service: inventory
  template:
    metadata:
      labels:
        app: store
        service: inventory
        release: "1.2.0"
    spec:
      containers:
        - name: inventory
          image: hellodk/echo-service:v1.2.0
          ports:
            - containerPort: 8080
          env:
            - name: SERVICE_NAME
              value: store-inventory
            - name: APP_NAME
              value: store
            - name: RELEASE_VERSION
              value: "1.2.0"
            - name: IMAGE_TAG
              value: "v1.2.0"
          envFrom:
            - configMapRef:
                name: store-inventory-config
            - secretRef:
                name: store-inventory-secrets
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
