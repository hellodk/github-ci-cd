apiVersion: v1
kind: Service
metadata:
  name: store-api-gateway
  labels:
    app: store
    service: api-gateway
    release: "1.2.0"
spec:
  selector:
    app: store
    service: api-gateway
  ports:
    - port: 8080
      targetPort: 8080
