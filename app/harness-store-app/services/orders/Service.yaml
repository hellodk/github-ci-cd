apiVersion: v1
kind: Service
metadata:
  name: store-orders
  labels:
    app: store
    service: orders
    release: "1.2.0"
spec:
  selector:
    app: store
    service: orders
  ports:
    - port: 8080
      targetPort: 8080
