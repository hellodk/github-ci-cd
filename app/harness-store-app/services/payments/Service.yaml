apiVersion: v1
kind: Service
metadata:
  name: store-payments
  labels:
    app: store
    service: payments
    release: "1.2.0"
spec:
  selector:
    app: store
    service: payments
  ports:
    - port: 8080
      targetPort: 8080
