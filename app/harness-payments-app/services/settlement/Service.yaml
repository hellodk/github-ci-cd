apiVersion: v1
kind: Service
metadata:
  name: pay-settlement
  labels:
    app: payments
    service: settlement
    release: "1.2.0"
spec:
  selector:
    app: payments
    service: settlement
  ports:
    - port: 8080
      targetPort: 8080
