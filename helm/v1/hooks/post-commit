#!/usr/bin/env bash
# helm/v1/hooks/post-commit — org-hooks helm v1 (non-blocking)
set -euo pipefail
branch="$(git rev-parse --abbrev-ref HEAD 2>/dev/null || echo unknown)"
printf '\033[0;32m[org-hooks]\033[0m helm commit on %s\n' "$branch" >&2
exit 0
